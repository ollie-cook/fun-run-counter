import RunCounter from "./components/RunCounter"

export default function Home() {
  return (
    <main className="min-h-screen flex justify-center bg-gradient-to-br from-cyan-400 to-sky-500">
      <RunCounter />
    </main>
  );
}
